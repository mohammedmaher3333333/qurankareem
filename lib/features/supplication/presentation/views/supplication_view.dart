import 'package:flutter/material.dart';

class SupplicationView extends StatelessWidget {
  const SupplicationView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Supplication');
  }
}
